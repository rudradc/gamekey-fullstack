server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown routes (e.g. /games/1) resolve to index.html
    # so Angular Router can handle them client-side.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
